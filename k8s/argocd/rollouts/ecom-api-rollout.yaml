apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: ecom-api
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 20
        - pause: { duration: 5m }
        - setWeight: 50
