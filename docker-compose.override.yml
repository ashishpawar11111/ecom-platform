# Intentionally empty.
#
# Docker Compose auto-loads docker-compose.override.yml. Keep this file as a
# no-op so `docker compose up` runs the production-like stack from
# docker-compose.yml.
#
# For Vite/dev mode, run:
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
version: '3.9'

services: {}
